<div class="col-1-5">
    <div class="form-group">
        <label for="{{ $id }}">{{ $label }}</label>
        <input type="text" name="{{ $name }}" class="form-control date advance_search form-control-sm"
            value="{{ $value }}" id="{{ $id }}" placeholder="Enter Date">
    </div>
</div>
